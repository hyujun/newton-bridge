# assets/_cache/

Auto-populated by `scripts/host/fetch_assets.sh`. Gitignored.

Contents today: a shallow clone of `mujoco_menagerie` (~2 GB) used as the
source of truth for Franka and KUKA MJCFs. Safe to delete at any time —
the fetch script will re-clone on next run.
